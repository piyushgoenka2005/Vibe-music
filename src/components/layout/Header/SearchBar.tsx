"use client";

/**
 * Classic search inputs live inside MainNav markup (#sw-search-input).
 * GlobalSearch binds to those inputs and renders the typeahead overlay.
 */
export default function SearchBar() {
  return null;
}
