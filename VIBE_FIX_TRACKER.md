# VIBE_FIX_TRACKER

Stack confirmed: **Next.js 16 + React 19 + Auth.js v5 + Prisma + PostgreSQL + Cloudinary/CDN**.

Baseline (local): Vitest **470/470 PASS** (2026-09-18). Production CWV **vibemusic.in (2026-09-21, mobile Lighthouse): perf 41, LCP 7.4s, TBT 1,400ms, CLS 0.111, payload 44.5MB — measured, root causes fixed, re-measure after deploy**.

| ID       | Area                | Issue                                                                                         | Severity | Root Cause                                                                                                        | Fix                                                                                                                                                                            | Test                           | Status                           |
| -------- | ------------------- | --------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------ | -------------------------------- |
| AUTH-01  | Google OAuth        | Dead-end `OAuthAccountNotLinked` / “try Google again to link”                                 | P0       | Prisma adapter detached from Auth.js; misleading error copy                                                       | Re-attach `createAuthPrismaAdapter`; keep email linking; rewrite error copy                                                                                                    | Auth unit + manual OAuth       | FIXED                            |
| AUTH-02  | Auth perf           | Extra `/api/auth/session` + double cart merge on login                                        | P1       | `credentialsSignIn` fetches session; `authStore` + `AuthProvider` both merge cart                                 | Use `getSession()` once; remove store-side cart merge                                                                                                                          | existing cart merge tests      | FIXED                            |
| CAT-01   | Search UX           | Empty state flash while products load                                                         | P0       | `status==="idle"` treated as ready-empty                                                                          | Treat idle+query/filter as loading; abort stale fetches                                                                                                                        | search hook behavior           | FIXED                            |
| CAT-02   | Search API          | Uncontrolled in-flight search requests                                                        | P1       | No `AbortSignal` on `fetch`                                                                                       | Wire abort through search service + hooks                                                                                                                                      | existing search tests          | FIXED                            |
| CAT-03   | Catalogue perf      | Brand/category browse loads full snapshot                                                     | P1       | `searchProducts` always `fetchCatalogSnapshot`                                                                    | Use `fetchProductsByBrandSlug` / `fetchProductsByCategory` for scoped browse                                                                                                   | catalog tests                  | FIXED                            |
| ADM-01   | Admin products      | Double fetch on every mount                                                                   | P1       | `staleTime:0` + invalidate on mount                                                                               | Remove mount invalidate; sensible staleTime                                                                                                                                    | manual                         | FIXED                            |
| ADM-02   | Admin refunds       | Status “refunded” ≠ Razorpay refund                                                           | P0       | Status flip without payment                                                                                       | Guard + partial refund UI (prior session)                                                                                                                                      | order service                  | FIXED                            |
| ADM-03   | Amazon import       | Legacy CSV only                                                                               | P0       | Template mismatch                                                                                                 | Amazon listing pipeline (prior session)                                                                                                                                        | amazonListingImport tests      | FIXED                            |
| IMG-01   | Images              | Oversized delivery                                                                            | P2       | CDN pipeline exists; measure live                                                                                 | Keep CDN optimize path; document hosting CDN                                                                                                                                   | image contract tests           | OPEN (code OK; measure prod)     |
| HOST-01  | CloudOnFire         | Server/CDN knobs                                                                              | P1       | Panel access only                                                                                                 | See `VIBE_HOSTING_ACTIONS_REQUIRED.md`                                                                                                                                         | N/A                            | BLOCKED                          |
| PAY-01   | Payments            | `failed` → `captured` race clobbered paid/failed state; inventory + coupon commits not atomic | P0       | Unconditional read-modify-write outside a transaction                                                             | Row-lock (`FOR UPDATE`) txn; CAS transitions; atomic coupon `updateMany`; `captured` promotes failed attempt to paid                                                           | type-check + 472 tests         | FIXED                            |
| PAY-02   | Refunds             | Double-submit issued duplicate partial refunds                                                | P1       | No idempotency guard                                                                                              | Per-order in-process refund mutex + skip already-refunded                                                                                                                      | type-check + 472 tests         | FIXED                            |
| PAY-03   | Razorpay            | SDK calls could hang request workers                                                          | P1       | No timeouts on `orders.create`/`payments.refund`                                                                  | `withTimeout` races (15s/20s)                                                                                                                                                  | type-check + 472 tests         | FIXED                            |
| EXT-01   | External calls      | Google Places/Nominatim fetches without timeout                                               | P1       | No `AbortSignal`                                                                                                  | `withTimeout` + 8s cap on address autocomplete/details/nominatim                                                                                                               | type-check + 472 tests         | FIXED                            |
| SEC-01   | CSRF                | `cart/reprice`, `orders/[id]/resume-payment` omitted mutation origin check                    | P1       | Missing `enforceMutationSecurity`                                                                                 | Added origin/CSRF guard to both routes                                                                                                                                         | existing mutation-origin tests | FIXED                            |
| SEC-02   | Socket.io           | Dead server accepted `order:track`/`admin:join` with no auth (latent data leak)               | P1       | Unreachable module with unauthenticated rooms                                                                     | Removed `src/lib/server/socket.ts` (no call sites)                                                                                                                             | grep verified                  | FIXED                            |
| SEC-03   | Uploads             | Review images trusted client MIME; garbage → 500                                              | P2       | No content sniffing                                                                                               | Magic-byte sniff (JPEG/PNG/WebP) → clean 400                                                                                                                                   | type-check + 472 tests         | FIXED                            |
| SEC-04   | Deploy              | Production env validation was commented out                                                   | P1       | `throw` disabled to unblock builds                                                                                | Restored throws (kept Resend SMTP fallback)                                                                                                                                    | type-check                     | FIXED                            |
| PERF-01  | LCP                 | Hero slide 0 was a 1.96MB PNG (`eager`+`high` priority)                                       | P0       | Raw asset in `public/`                                                                                            | Sharp→WebP (1920px, q82) → 126KB; `<picture>` pipes `srcOptimized`                                                                                                             | lcpBudget tests                | FIXED                            |
| PERF-02  | Cart reprice        | N+1: per-line `getProductById` DB call                                                        | P1       | No batch fetch                                                                                                    | `getProductsByIds` single `findMany({ in })` + in-memory map                                                                                                                   | existing reprice tests         | FIXED                            |
| PERF-03  | Search API          | `all=1` returned unbounded full-match payload                                                 | P1       | No ceiling                                                                                                        | `MAX_ALL_LIMIT=1500` + `truncated` flag                                                                                                                                        | search route tests             | FIXED                            |
| PERF-04  | Footer trending     | Recomputed on each request                                                                    | P2       | No cache                                                                                                          | Already `unstable_cache` 60s — verified, no change                                                                                                                             | footer tests                   | VERIFIED-OK                      |
| PERF-05  | Checkout validation | N+1: per-line `getProductById` in `resolveOrderItems`                                         | P1       | No batch fetch                                                                                                    | Reuse `getProductsByIds` single `findMany({ in })`                                                                                                                             | existing orderPlacement tests  | FIXED                            |
| PAY-04   | Verify payment      | Second read-modify-write `updateOrder` for signature after commit                             | P1       | Post-commit field write outside txn                                                                               | `razorpaySignature` folded into the payment transaction patch                                                                                                                  | type-check + 472 tests         | FIXED                            |
| CLEAN-01 | Dead code           | Superseded `transitionOrderPaymentStatus`/`paymentPatchToPrisma` left unused                  | P2       | Row-lock design replaced CAS                                                                                      | Removed helper + public wrapper                                                                                                                                                | type-check + lint              | FIXED                            |
| PAY-05   | Order writes        | Remaining read-modify-write field updates outside the txn (`updateOrder`, `patchOrderFields`) | P2       | fetch-merge-write on single/adjacent columns                                                                      | `updateOrderFields` atomic `updateMany`; `patchOrderFields` writes patched columns only, then re-reads; removed all `updateOrder` full-object RMW                              | type-check + lint + 472 tests  | FIXED                            |
| IMG-01   | Images              | Oversized delivery                                                                            | P2       | CDN pipeline exists; measure live                                                                                 | **Measured live 2026-09-21**: 8.5MB/7.6MB/6.2MB CDN PNG masters were being fetched on the homepage (raw `product.image`), ~22MB total; root causes fixed under IMG-02/VIDEO-01 | type-check + lint + 472 tests  | CLOSED (re-measure after deploy) |
| IMG-02   | Homepage marquee    | Scanner cards rendered raw CDN PNG masters (~22MB)                                            | P1       | `toScannerProduct` passed catalog `image` (PNG master) straight to `<img>`                                        | `storefrontImageUrl(image, 480)` → `-w480.webp` derivative (~35–100KB) in `findYourProductTracks`                                                                              | type-check + lint + 472 tests  | FIXED (verify-after-deploy)      |
| VIDEO-01 | Reels               | ~20MB of reel mp4s downloaded on page load (reel-2 17MB, reel-1 5.9MB)                        | P1       | `useVisibleVideo` measured ONLY horizontal strip overlap — cards below the fold counted as visible and autoplayed | Vertical viewport bound added to `isVisibleInStrip`/retry loop; `<video src>` not mounted until card is within 500px of viewport                                               | type-check + lint + 472 tests  | FIXED (verify-after-deploy)      |

P0 open in-code: **0**. P1 blocked (hosting): **HOST-01**. Verify-after-deploy: IMG-02, VIDEO-01 (run `npm run audit:lighthouse` against `https://vibemusic.in`).
